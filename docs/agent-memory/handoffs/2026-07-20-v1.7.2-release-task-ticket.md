# Task Ticket: v1.7.2 Release

- ID: RELEASE-V1.7.2
- Status: `done`
- Owner: `Claude Code` preparation, `Codex` publication
- Source: User authorization on 2026-07-20

## Objective

Prepare, verify, commit, push, and publish the completed transcript keyword-search feature as patch version `1.7.2` through the existing trusted-publishing workflow.

## Scope

In scope:

- Bump package and lockfile to `1.7.2`.
- Promote both changelog `Unreleased` entries to `1.7.2` dated 2026-07-20.
- Update both README release links to `v1.7.2`.
- Run local release gates, create one focused commit, push `master`, push annotated `v1.7.2`, monitor Actions/npm, and create a non-draft GitHub Release.

Out of scope:

- Runtime expansion, dependency/workflow changes, unrelated cleanup, npm token use, PR creation, or history rewriting.

## Scope Boundary

Exclude the pre-existing generated change in `docs/agent-memory/pending-learning-proposals.md`. Include only the transcript-search feature, its planning/research/verification artifacts, and the v1.7.2 release preparation.

## Acceptance Criteria

- [x] Package, lockfile, built server metadata, README links, and changelogs agree on `1.7.2`.
- [x] Full tests, build, production audit, package dry run, stdio smoke, UTF-8, diff, and secret gates pass.
- [x] One focused commit reaches `origin/master`; no unrelated generated queue change is included.
- [x] Annotated tag `v1.7.2` triggers a successful trusted npm publication.
- [x] npm latest is `1.7.2` with provenance and the published CLI smoke passes.
- [x] A non-draft, non-prerelease GitHub Release exists for `v1.7.2`.

## Stop And Report Conditions

Stop before tagging if any local gate fails, `v1.7.2` already exists, secrets are detected, origin/master diverges, or a workflow/dependency change becomes necessary.

## Completion Note

Released from commit `b05001b` through Actions run `29728674803`. npm latest and provenance, the exact-version CLI smoke, and the non-draft GitHub Release were verified after publication.
