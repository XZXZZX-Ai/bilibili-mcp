# Codex To Claude Handoff: v1.7.1 Release Verification

## Objective

Make the minimum documentation adjustment needed for the authorized `v1.7.1` publication, then perform an independent release-readiness review. Codex retains all Git, tag, npm, GitHub Actions, and GitHub Release operations.

## Current State

- `master` is at `9231ae2`, package/lock version `1.7.1`.
- npm latest is `1.7.0`; local/remote `v1.7.1` and GitHub Release `v1.7.1` do not exist.
- `.github/workflows/publish.yml` publishes on `v*.*.*`, using Node `22.14.0`, npm `11.18.0`, OIDC `id-token: write`, tests, build, and `npm publish --provenance --access public`.
- The only unrelated worktree change is the generated date in `docs/agent-memory/pending-learning-proposals.md`; preserve it and do not stage or revert it.

## Files To Inspect

- `package.json`, `package-lock.json`, `.github/workflows/publish.yml`
- `README.md`, `README_EN.md`, `CHANGELOG.md`, `CHANGELOG_EN.md`
- `docs/agent-memory/active-work.md`, `harness-security.md`, `harness-eval.md`

## Files To Edit

- `README.md`, `README_EN.md`: change the top Release link from `v1.7.0` to `v1.7.1` only.
- `CHANGELOG.md`, `CHANGELOG_EN.md`: delete the `1.7.1` bullet saying npm/GitHub still remain at `1.7.0`; do not replace it with an unverified publication claim.
- `docs/agent-memory/active-work.md`: mark the bounded `v1.7.1` release execution active without claiming publication succeeded.
- Write `docs/agent-memory/handoffs/2026-07-20-v1.7.1-release-claude-report.md`.

## Required Capability

- Use the project `release-verifier` subagent/capability for final release gates.
- Use `risk-reviewer` only as a bounded post-check; if it stalls, report the gap instead of waiting indefinitely.
- Use package-maintenance checks directly; no package change is expected.

## Constraints

- Do not change version, runtime, dependencies, tests, workflows, package scripts, or generated `dist/`.
- Do not commit, stage, push, tag, publish, create a release, or close issues.
- Do not print credentials, Cookies, npm/GitHub tokens, or `.env` content.
- Do not use Superpowers.

## Execution Steps

1. Read the companion ticket, current diff, and exact release workflow.
2. Make only the specified README/changelog/active-work edits.
3. Run the verification commands and validate package entry points and tarball exclusions.
4. Confirm eight MCP tool schemas and compiled server version `1.7.1`.
5. Scan added content for high-confidence secrets and verify strict UTF-8.
6. Return the report with `Harness Artifacts` and explicit skipped/live checks.

## Verification Commands

```bash
npm ci
npm run build
npm test
npm audit --omit=dev --json
npm pack --dry-run --json
git diff --check
```

## Acceptance Criteria

- Release docs are future-proof for the imminent `v1.7.1` publication.
- All local gates pass with 244 tests and zero production vulnerabilities.
- Tarball version/entry points/content are correct and secret-free.
- No unauthorized external mutation occurs.

## Stop And Report If

- A gate fails, a release object already exists, a credential is found, or any source/workflow repair is necessary.
