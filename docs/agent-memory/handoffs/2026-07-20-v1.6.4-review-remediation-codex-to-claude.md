# Codex To Claude Handoff: v1.6.4 Review Remediation

## Objective

Close two pre-release verification gaps found while reviewing the uncommitted Issues #2-#15 and harness changes.

## Current State

- Repository: `C:\Users\ZX\bilibili-mcp`
- Fixed point: `HEAD` / `v1.6.3`
- Existing full suite passes with 20 files and 178 tests.
- Do not discard or rewrite any existing worktree changes.

## Files To Inspect

- `src/bilibili/wbi.ts`
- `tests/bilibili-wbi.test.ts`
- `.codex/scripts/stop_summary.py`
- `docs/agent-memory/handoffs/2026-06-18-harness-artifact-stop-reminders-task-ticket.md`
- `docs/agent-memory/harness-security.md`

## Files To Edit

- `tests/bilibili-wbi.test.ts`
- Add one minimal stdlib-only Python test file next to `stop_summary.py` if needed.
- `docs/agent-memory/handoffs/2026-06-18-harness-artifact-stop-reminders-task-ticket.md`
- `docs/agent-memory/handoffs/2026-07-20-v1.6.4-review-remediation-claude-report.md`

Do not edit production TypeScript unless a new regression proves a real defect; if so, stop and report instead.

## Required Capability

- Use the `vitest` skill for TypeScript tests.
- Use the project `test-baseline-builder` subagent for test review.
- Use `risk-reviewer` for the final focused review.
- Do not use Superpowers.

## Constraints

- Add a deterministic regression proving WBI HTTP 503 is retried four times and preserves status 503.
- Add a deterministic regression proving repeated WBI `AbortError` is retried according to existing policy and finally becomes `TimeoutError` without changing timeout duration.
- Add the smallest runnable stdlib-only check covering `stop_summary.py` path normalization/matching and all three reminder branches. Do not add dependencies or execute Git writes.
- Validate the hook stdout remains exactly one JSON control object and that only approved runtime observation files are written.
- Mark the harness task ticket complete only if every listed acceptance criterion is directly verified.
- Never print or store credentials, Cookie values, npm tokens, or GitHub tokens.
- No commit, push, tag, release, issue mutation, version bump, changelog edit, or package publish.

## Execution Steps

1. Add failing-first WBI regressions, then confirm current production code passes them.
2. Add the minimal Python check for the new hook branching logic.
3. Run focused checks, full tests, build, Python syntax/checks, and `git diff --check`.
4. Review against harness-security and update the harness ticket status/checklist only when proven.
5. Write the report with exact command results and Harness Artifacts status.

## Verification Commands

```powershell
npm test -- --run tests/bilibili-wbi.test.ts
npm test
npm run build
python -m py_compile .codex/scripts/stop_summary.py
python <new-test-file>
python .codex/scripts/stop_summary.py --agent codex
git diff --check
```

## Acceptance Criteria

- WBI 403 remains one attempt.
- WBI 503 makes four attempts and preserves `statusCode: 503`.
- Status-less `TypeError` makes four attempts and remains `NetworkError`.
- `AbortError` makes four attempts and the final public error is `TimeoutError`.
- Hook reminder branches have a runnable deterministic test.
- Hook stdout remains JSON-safe.
- Full build and test suite pass.
- No production behavior, dependency, public MCP schema, version, Git, or release state changes.

## Things Not To Change

- Do not edit `src/` unless a regression proves a defect and Codex approves a repair.
- Do not alter Cookie/credential behavior.
- Do not stage or commit files.
- Do not modify generated `pending-learning-proposals.md`.

## Stop And Report If

- Current production code fails either new WBI invariant.
- Hook verification writes outside approved runtime observation locations.
- A new dependency or broader redesign appears necessary.

## Expected Claude Report

Write `docs/agent-memory/handoffs/2026-07-20-v1.6.4-review-remediation-claude-report.md` using the repository report template, including files changed, commands, results, risks, skipped checks, decision points, subagents used, and Harness Artifacts.
