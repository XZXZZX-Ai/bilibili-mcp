# Codex To Claude Handoff: v1.6.5 Source Preparation

## Update Goal

Prepare the completed MCP server-version synchronization fix as patch version `1.6.5` so Codex can commit and push it to `origin/master`.

## Current Judgment

The live npm registry reports `latest: 1.6.4`. The local publish workflow triggers only on `v*.*.*` tags or manual dispatch, so a normal master push will not publish npm. This task must prepare source metadata only; tagging and publication remain separate unauthorized gates.

## Recommended Approach

- Follow `docs/agent-memory/handoffs/2026-07-20-v1.6.5-preparation-task-ticket.md`.
- Use `npm version 1.6.5 --no-git-tag-version` so both package files remain synchronized without a commit or tag.
- Add minimal clean UTF-8 Chinese and English changelog entries for the MCP server metadata version fix and regression verification.
- Run the full package/release checks, then write the required report.

## Things To Avoid

- Do not change dependencies, publish workflow, source/test implementation, README, `dist/`, or the published-version fact that npm latest remains `1.6.4` until an actual release.
- Do not touch the pre-existing `docs/agent-memory/pending-learning-proposals.md` modification.
- Do not create a commit, tag, push, npm publish, GitHub Release, or PR.
- Do not copy terminal mojibake into files; write clean UTF-8 text and verify it explicitly.

## Claude Code Execution Steps

1. Inspect the task ticket, package metadata, changelog style, publish workflow trigger, and current scoped diff.
2. Use/name the `package-maintainer` subagent for the version and package-content work.
3. Run `npm version 1.6.5 --no-git-tag-version`.
4. Add concise `1.6.5` entries to `CHANGELOG.md` and `CHANGELOG_EN.md`.
5. Run `npm run build`, `npm test`, `npm audit --omit=dev --json`, `npm pack --dry-run --json`, and `git diff --check`.
6. Run a scoped high-confidence secret/credential-pattern scan over the intended commit files without printing any secret value.
7. Use/name `release-verifier` for the final bounded readiness review.
8. Write `docs/agent-memory/handoffs/2026-07-20-v1.6.5-preparation-claude-report.md` with exact results and the required Harness Artifacts section.

## Acceptance Criteria

- Package and lockfile version are `1.6.5`; dependency graph is unchanged.
- Changelog entries are bilingual, minimal, accurate, and valid UTF-8.
- All required checks pass with no high-confidence secret finding.
- No release, tag, commit, push, dependency, workflow, or unrelated-file change occurs.

## Risks

- Accidentally including the generated learning-proposal date change in the commit.
- Treating source preparation as authorization for a tag or npm publication.
- Terminal display encoding being mistaken for file corruption.

## Stop And Report If

Stop on a real secret, unexplained verification failure, dependency/workflow change, or need for a tag/publish action.

## Expected Claude Report

Include changed files, exact commands/results, package contents summary, secret-scan result without values, release-verifier result, skipped checks, decision points, and Harness Artifacts status.
