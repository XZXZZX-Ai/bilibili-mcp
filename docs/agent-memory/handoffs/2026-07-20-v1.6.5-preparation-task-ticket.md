# Task Ticket: Prepare v1.6.5 Source State

## Ticket

- ID: local-2026-07-20-v1.6.5-preparation
- Title: Bump the package patch version for the MCP metadata fix
- Status: `done`
- Owner: `Claude Code`
- Source: User request to commit and push a patch-version update

## Objective

Prepare the current MCP server-version fix as package version `1.6.5`, with synchronized package metadata and concise Chinese/English changelog entries, ready for Codex verification, commit, and push.

## Scope

In scope:

- Bump `package.json` and `package-lock.json` from `1.6.4` to `1.6.5` using npm tooling.
- Add matching `1.6.5` entries to both changelogs describing the MCP metadata fix and 181-test verification.
- Run build, test, production audit, and package dry-run checks.

Out of scope:

- Creating or pushing a `v1.6.5` tag.
- Running `npm publish`, triggering the publish workflow, or creating a GitHub Release.
- Changing the publish workflow, dependencies, public tools, or implementation beyond the already completed version fix.
- Committing or pushing; Codex owns Git publication after review.

## Files To Inspect Or Edit

Expected inspect:

- `package.json`, `package-lock.json`
- `CHANGELOG.md`, `CHANGELOG_EN.md`
- `.github/workflows/publish.yml`
- Current source/test diff and prior version-fix handoffs

Expected edit:

- `package.json`
- `package-lock.json`
- `CHANGELOG.md`
- `CHANGELOG_EN.md`

Do not touch:

- `docs/agent-memory/pending-learning-proposals.md`
- `.github/workflows/publish.yml`, dependencies, `dist/`, credentials, tool schemas, or source/test implementation

## Required Capabilities

- Claude Code subagent: `package-maintainer` for version/changelog/package checks
- Claude Code subagent: `release-verifier` for final readiness review
- Skill/constraint: `secret-scanning` for a scoped pre-commit credential-pattern scan
- CLI: `npm version`, `npm run build`, `npm test`, `npm audit --omit=dev`, `npm pack --dry-run --json`, `git diff --check`

## Acceptance Criteria

- [ ] Both package metadata files report `1.6.5` with no dependency changes.
- [ ] Both changelogs contain clean UTF-8 `1.6.5` entries describing the version-source fix.
- [ ] Build and all tests pass.
- [ ] Production dependency audit reports no vulnerabilities.
- [ ] Package dry-run contains the expected package files and excludes internal handoffs, tests, and credentials.
- [ ] Scoped secret scan finds no high-confidence credential material in the intended commit.
- [ ] No tag, publish, GitHub Release, commit, or push is performed by Claude Code.

## Verification

```bash
npm run build
npm test
npm audit --omit=dev --json
npm pack --dry-run --json
git diff --check
```

## Stop And Report Conditions

Stop if verification requires a dependency/workflow change, a real secret is found, npm tooling changes more than the two version fields, or release publication/tagging would be required.
