# Task Ticket: v1.7.1 Release

- ID: RELEASE-V1.7.1
- Status: `in_progress`
- Owner: `Claude Code`
- Source: User authorization on 2026-07-20

## Objective

Prepare and independently verify the existing `1.7.1` source for its tag-triggered trusted npm publication and GitHub Release.

## Scope

In scope:

- Update the bilingual README release links from `v1.7.0` to release target `v1.7.1`.
- Remove the now-obsolete source-only caveat from both `1.7.1` changelog entries.
- Run all local release gates and report exact evidence.
- Record the bounded release handoff and verification status.

Out of scope:

- Runtime, dependencies, tests, workflow YAML, tag creation, npm publication, or GitHub Release creation by Claude Code.

## Files To Inspect Or Edit

Expected edit: `README.md`, `README_EN.md`, `CHANGELOG.md`, `CHANGELOG_EN.md`, `docs/agent-memory/active-work.md`, and the release report.

Do not touch: `docs/agent-memory/pending-learning-proposals.md`, package/runtime/test/workflow files, credentials, or generated `dist/`.

## Acceptance Criteria

- [ ] Both READMEs point at `v1.7.1`.
- [ ] Changelogs no longer claim npm latest remains `1.7.0`.
- [ ] Package/lock/server metadata all report `1.7.1`.
- [ ] Build, 244 tests, production audit, package dry run, entry-point, diff, UTF-8, and secret gates pass.
- [ ] Claude Code does not commit, push, tag, publish, or create a GitHub Release.

## Verification

```bash
npm ci
npm run build
npm test
npm audit --omit=dev --json
npm pack --dry-run --json
git diff --check
```

## Stop And Report Conditions

Stop if any required gate fails, the release/tag/version already exists remotely, a secret is found, or a source/workflow change is required.
