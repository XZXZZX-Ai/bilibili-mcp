# v1.14.0 release execution

## Scope and authority

User explicitly requested a new version, bilingual changelog/Release, tag and npm publication. Codex Direct owns the bounded release from the isolated qr-login-t1 worktree. Publish accepted QR login (#76–#78), comment time-sort fix (#74), and existing unreleased Apache-2.0/subtitle fallback changes. Update five version fields to 1.14.0. Preserve the dirty primary checkout and machine-local Skills. No force push, credential collection or unrelated issue work.

## Release path and verification

Reuse the existing tag-triggered .github/workflows/publish.yml: GitHub-hosted Ubuntu, Node 22.14.0, npm 11.18.0, OIDC and provenance. Current official npm trusted-publishing documentation requires npm >=11.5.1 and Node >=22.14.0; current workflow meets those requirements. Sources checked 2026-09-12: https://docs.npmjs.com/trusted-publishers/ and https://docs.github.com/en/actions/tutorials/publish-packages/publish-nodejs-packages . No workflow change required; remote publication outcome is verified through Actions and registry metadata.

Run build/full tests, built CLI smoke, pack allowlist and production audit, version/secret checks, and bounded release-verifier review. Fast-forward remote master from observed 7d60a9e only if still an ancestor; do not move the checked-out local master in the dirty primary directory. Push version tag after acceptance, monitor publish, create bilingual GitHub Release and verify npm version/provenance/CLI. If publication fails, inspect actual logs; do not reuse a published version or rewrite tags.

## Known limitations

Real phone confirmation and native three-platform QR readability remain unverified. The user closed #79 and subsequently explicitly authorized release; this release discloses that limit instead of claiming the checks passed. No automatic renewal or multiple-account management.

## Status

Build, 50 test files / 1264 tests, and 8 built CLI scenarios passed after compatible transitive dependency updates. Production audit: zero vulnerabilities (initial fast-uri/hono/qs findings resolved). Pack: 197 allowlisted files; no secrets or harness artifacts. release-verifier reviewed metadata, MCP compatibility, changelog parity, workflow and package contents. Migration fixture package manifest refreshed from actual pack output using existing canonical newline rules; evaluator unchanged. Remote publication pending.
