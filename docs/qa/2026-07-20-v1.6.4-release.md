# QA Session: v1.6.4 Release

- Date: 2026-07-20
- Version or commit: v1.6.4 candidate
- Owner: Codex
- Related tickets: GitHub Issues #2-#15
- QA type: pre-release / package-install / regression

## Scope

In scope:

- Source, test, harness, package, stdio, credential-safety, and release gates for v1.6.4.
- npm package contents and trusted-publishing workflow readiness.

Out of scope:

- Real Cookie values or private account data.
- Exhaustive client matrix and live Bilibili availability.

## Preconditions

- [x] Branch and upstream confirmed as `master` / `origin/master`.
- [x] GitHub and npm latest confirmed as v1.6.3 before release.
- [x] GitHub CLI authentication confirmed without recording token values.
- [ ] Final v1.6.4 commit and tag recorded.

## Automated Baseline

- [ ] `npm ci`
- [ ] `npm run build`
- [ ] `npm test`
- [ ] `npm pack --dry-run --json`
- [ ] MCP stdio smoke and `tools/list`
- [ ] `npm audit --omit=dev`
- [ ] Secret-pattern scan over staged files
- [ ] `git diff --check`

## Package And Install Path

- [ ] `package.json` and `package-lock.json` both report 1.6.4.
- [ ] `main`, `module`, `types`, and `bin` target built `dist` output.
- [ ] Pack contains only expected publish files and excludes tests, `.env`, `.claude`, `.codex`, and internal docs.
- [ ] Post-publish `npm view` reports latest 1.6.4.
- [ ] `npx -y @xzxzzx/bilibili-mcp@1.6.4 --help` succeeds.

## MCP And Security

- [ ] Seven expected MCP tools remain discoverable.
- [ ] Stdio startup produces no non-JSON stdout before protocol traffic.
- [ ] Credential setup/status responses do not expose Cookie values.
- [ ] Validation and structured error response tests pass.
- [ ] No high-confidence credential, private-key, npm-token, or GitHub-token pattern appears in staged content.

## Release Workflow

- [x] Workflow uses `contents: read` and `id-token: write`.
- [x] Workflow configures the npm registry and publishes with `--provenance --access public`.
- [x] Workflow installs an npm CLI version supporting trusted publishing.
- [ ] Tag-triggered GitHub Actions run succeeds.
- [ ] npm provenance and registry metadata are visible after publish.
- [ ] GitHub Release exists and is not draft or prerelease.

## Client Compatibility

| Client | Result | Notes |
|---|---|---|
| Codex | not tested | Post-release MCP reconnect is outside automated package gates. |
| Claude Desktop | not tested | No local client session exercised. |
| Cursor | not tested | No local client session exercised. |

## Result

- Overall result: pending
- Blocking issues: final security remediation and release gates pending
- Non-blocking caveats: live Bilibili and client matrix checks are not automated
- Codemap: checked; update only if final release work changes navigation
