# ASR setup prompt fix and v1.14.1 release

## Ticket and execution brief

User invoked implement with Codex Direct and explicitly authorized commit, push, tag, bilingual changelogs/GitHub Release and npm publication. Canonical worktree: qr-login-t1; base ff3a166e1ef976c3eebd40dbbdc0ea42c3ac60d4. Single Codex writer; preserve dirty primary checkout and managed Skill links.

Objective: interactive setup must recognize installed ASR. Ready installations display model/device and default to reuse without invoking installer; explicit reconfiguration retains model/device selection. Absent installations offer installation; incomplete or unverified legacy installations offer repair/verification without claiming readiness. Credential failure still stops before ASR; noninteractive flags retain current behavior. No real model installation or credential mutation during tests.

Files: src/cli.ts, focused CLI regression tests, bilingual changelogs, package/version metadata, mechanical migration receipts and release records. Reuse readAsrState and existing execution profiles. No installer, credential, MCP schema or workflow changes. Rollback via scoped revert; never rewrite released tags. Stop for a material behavior decision or failed publication requiring wider changes.

Verification: test-first regression, focused CLI tests, build/full tests, CLI smoke, production audit, pack allowlist/entrypoints, fixture conformance, code-review and release-verifier, GitHub CI, npm provenance and clean-directory installed version. Skills: implement, tdd, vitest, code-review, secret-scanning, git-publish; package-maintainer responsibility stays with authorized Codex Direct. Existing Node/TypeScript build path and publishing workflow retained.

## Results

Red test confirmed the old unconditional install prompt. After the fix: 102 focused CLI tests, build/typecheck, 51 files / 1273 full tests passed. Built CLI smoke initially found ambient ASR state in the smoke harness; isolating HOME/USERPROFILE in a temporary directory restored all 8 scenarios. Real local ASR state read-only exercise displayed small/cpu/int8 and reused it without running the installer. Production audit: zero vulnerabilities. Pack allowlist: 197 files; metadata and entrypoints checked. Bounded release-verifier accepted Standards and Spec, including CPU fallback readiness. Fixture receipt refreshed mechanically; remote release completed. No real model installation or credential writes.


## Publication

Release commit 8bfac926510a0eac01f7b126570779816d6b5f58; annotated v1.14.1 tag peels to that commit. Verify run 34691719018 and trusted-publish run 34692223065 succeeded. Public/latest GitHub Release has Chinese and English notes; npm latest=1.14.1 with SLSA provenance. Exact-version npx outside the checkout prints 1.14.1. No credentials or ASR installation modified. Official MCP Registry publication was outside scope.
